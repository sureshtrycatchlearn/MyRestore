import { Typography } from "@mui/material";

export default function CheckoutPage(){
    return(
        <Typography variant="h3">
            Only Logged in users should able to see this page!
        </Typography>
    )
}